{
  "name": "week6-sequence-models-attention",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --port 3000",
    "start": "vite build && vite preview --port 3000",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "test": "vitest run",
    "ci": "vitest run && vite build"
  },
  "dependencies": {
    "@preact/preset-vite": "^2.10.5",
    "lucide-preact": "^1.23.0",
    "preact": "^10.29.4",
    "three": "^0.174.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "happy-dom": "^20.10.6",
    "vite": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
